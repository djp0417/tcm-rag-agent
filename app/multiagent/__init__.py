# -*- coding: utf-8 -*-
"""多 Agent 协作流水线（阶段 4）：问诊 → 辨证 → 方案 → 安全审查 → 主编。

模块划分（与 docs/阶段4-多Agent设计方案.html 一一对应）
--------------------------------------------------------
    state.py     TCMState 共享状态 + 累积型字段的 reducer（图的"血液"）
    prompts.py   各角色提示词（每个角色只关心一件事，短而准）
    nodes.py     节点实现（图上的每个方框）
    graph.py     StateGraph 装配、条件边、否决回环
    __main__.py  命令行入口（Step 1 联调用）

与现有单 Agent 的关系
---------------------
**不是替换，是加一条更贵的路。** 闲聊与单点问答继续走 `app/agent`（问诊）
与 `app/rag.RAGSession`（问答）的快路径；只有「完整体质辨识后的调理方案」
才进本流水线，红旗症状直接短路到建议就医。

注意：本文件刻意不 import 子模块——nodes/graph 之间存在相互引用，
在此提前导入会触发循环导入（与 app/agent/__init__.py 同一约定）。
"""
